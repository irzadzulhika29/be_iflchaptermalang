<!DOCTYPE html>
<html>
<head>
    <title>{{ $subject }}</title>
</head>
<body>
    <h2>{{ $subject }}</h2>
    <p>{{ $notifMessage }}</p>
    <p>Error Message :</p>
    <p>{{ $errorMessage }}</p>
    <p>Please take appropriate action to resolve this issue and try to contact the user : {{ $userEmail }}</p>
</body>
</html>
